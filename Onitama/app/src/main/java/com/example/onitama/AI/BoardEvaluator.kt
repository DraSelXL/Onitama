package com.example.onitama.AI

import com.example.onitama.components.Block
import com.example.onitama.components.Card

/**
 * A class that holds an evaluation function to determine a player's advantage.
 */
class BoardEvaluator {
    companion object {
        fun evaluate(board: MutableList<MutableList<Block>>, cards: Array<Card>, team: String) {
            for (i in board.indices) {
                for (j in board[i].indices) {

                }
            }
        }
    }
}